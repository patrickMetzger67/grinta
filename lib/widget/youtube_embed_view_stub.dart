import 'package:flutter/widgets.dart';

/// Non-web stub — mobile uses [WebView] instead.
Widget buildYoutubeEmbedView({
  required String videoId,
  required String viewType,
}) {
  return const SizedBox.shrink();
}
