/**
 * Gemini chat configuration (Cloud Functions).
 * Keep in sync with `lib/config/gemini_chat_config.dart` (kGeminiChatModel).
 */

/** Gemini model for chat (generateContent API). */
const GEMINI_CHAT_MODEL = 'gemini-2.5-flash';

module.exports = {
  GEMINI_CHAT_MODEL,
};
